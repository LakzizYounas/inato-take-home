import { describe, expect, test } from 'vitest';
import { contextsClinicalTrials } from '../../../../libs/contexts/clinical-trials/src';

describe('', () => {
  test('', () => {
    expect(contextsClinicalTrials()).toBe('contexts-clinical-trials');
  });
});
